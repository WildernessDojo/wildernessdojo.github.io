WILDERNESS DOJO — FIXED STATIC SITE

Upload index.html and the assets folder to the root of the
WildernessDojo/wildernessdojo.github.io repository.

This package repairs the literal [...] truncation markers that broke the
original CSS and HTML. The StarLens and ARU Dojo links expect their existing
files to remain in the repository.
